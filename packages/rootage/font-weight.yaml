kind: Tokens
metadata:
  id: font-weight
  name: Font Weight
data:
  collection: global
  tokens:
    $font-weight.regular:
      values:
        default: 400
    $font-weight.medium:
      values:
        default: 500
    $font-weight.bold:
      values:
        default: 700
